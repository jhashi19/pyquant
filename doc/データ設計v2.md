### 0. 設計規約
- すべての日時は UTCテキスト（`YYYY-MM-DD` 等）。市場の暦日解釈は `calendar_def.time_zone` で行う。
- 数値は SQLite の REAL。
- 列挙は CHECK で固定。参照整合は FK を付与。
- 監査列は原則 `created_at`。追記主義で上書きを避ける。
- 市場日付の決定や T+N 起算では TZ とカレンダーを用いる。年率時間は 日数計算で扱う。

### 1.参照データ
#### 1.1 `currency`（通貨マスタ）

##### 説明（テーブル）
表示・丸め規則、FXスポット既定ラグ、通貨既定カレンダーを保持。期日計算や表示、換算の基底情報。<br>
表示の原則：識別と計算は常に ccy（ISO）。人間向け表示で name／symbol を用途分け。
| 列名             | 型                | 必須 | 説明                |
| -------------- | ---------------- | -- | ---------------------------------------- |
| `ccy`            | TEXT (PK)        | Y  | ISO4217 3文字。全テーブルの通貨FKに使用。集計の軸。例：`'JPY'`、`'USD'`。                                               |
| `name`           | TEXT             | Y  | 通貨の正式名称（ローカライズ可）。UIのリストやラベルで使用。例：`'Japanese Yen'` / `'円'`、`'US Dollar'` / `'米ドル'`。              |
| `iso_numeric`    | INTEGER (UNIQUE) |    | 外部ベンダーやCSV連携時の同定キー。例：JPY=`392`、USD=`840`。                                                           |
| `minor_unit`     | INTEGER          | Y  | 金額表示の丸め桁。UI/帳票のフォーマッタで使用。例：JPY=`0`、USD=`2`。                                                         |
| `symbol`         | TEXT             |    | 金額の書式用記号。単一通貨画面で使用。例：JPY=`'¥'`（`'￥'`もあり）、USD=`'$'`。 |
| `spot_lag`       | INTEGER          | Y  | FXスポット決済ラグ営業日（デフォルト=`2`）。T+N起算に使用。                                                                   |
| `default_cal_id` | TEXT             |    | 既定の営業日カレンダー。取引でカレンダー未指定時のフォールバック。決済日の営業日調整に使用。                                |
| `enabled`        | INTEGER          | Y  | 論理有効フラグ。廃止通貨を物理削除せずに非表示化。履歴と参照整合を維持。               |
| `valid_from`     | TEXT             | Y  | 通貨定義の適用開始。`minor_unit`や`spot_lag`変更の有効時点を明示。履歴管理。                                 |
| `retired_at`     | TEXT             |    | 廃止日。新規計上や取込時のバリデーションに使用（期間オーバーを即検知）。                                     |
| `created_at`     | TEXT             | Y  | 監査用タイムスタンプ（UTC）。                                         |
##### 使用目的
- 取引通貨など、システム内で使用する通貨はすべてこのテーブルにあるものを使用する。
- 各通貨の表示やスポットラグなど、通貨ごとのメタデータもカラムとして持ち、システム内のUIや計算で使用する。
---
#### 1.2 `currency_calendar`（通貨とカレンダーの紐付け）
##### 説明（テーブル）
通貨に対し、既定・決済・休日専用など役割別の市場カレンダーを割り当てる。
| 列名     | 型                  | 必須 | 説明                             |
| ------ | ------------------ | -- | ---------------------------------------------------------- |
| `ccy`    | TEXT (FK→currency) | Y  | 対象通貨を指定。                                   |
| `cal_id` | TEXT               | Y  | 使用する市場カレンダーID。休日集合を表す識別子（`calendar.cal_id`）。<br>例：`'USNY'`（米国NY市場）, `'JPTO'`（日本東京市場）。<br>補足：SQLiteは複合主キーの部分参照にFK制約を張れないため、この列は実質的にドメイン制約。            |
| `role`   | TEXT (CHECK)       | Y  | カレンダーの用途区分。1通貨に複数のカレンダーを役割別に持てる。<br>`DEFAULT`=取引・カーブでカレンダー未指定時の既定。期日生成や営業日調整の初期値。<br>`SETTLEMENT`=FXスポットラグなど決済日の営業日調整に用いる基準。<br>`HOLIDAY_ONLY`=休日集合としての参照用途。単独で営業日調整に使わない想定。<br>roleを増やすならCHECKを拡張（例：`'FIXING'`, `'PAYMENT'` など）か、roleを別テーブルで管理。  |
##### 使用目的
- 通貨とカレンダー（休日集合）を紐づける。
---
#### 1.3 `daycount`（日数計算）
##### 説明（テーブル）
日数計算の方式を登録する。
| 列名           | 型             | 必須 | 説明               |
| ------------ | ------------- | -- | -------------------------- |
| `code`         | TEXT (PK)     | Y  | 人間・機械共通のキー。例: `ACT/360`, `ACT/365F`, `30E/360`, `ACT/ACT-ISDA`。 |
| `display_name` | TEXT          | Y  | 表示名。例: `Actual/365 Fixed`。                  |
| `formula_tag`  | TEXT (UNIQUE) | Y  | 実装を一意に決める識別子。例:`ACT_360`, `ACT_365F`, `ACT_365L`, `ACT_ACT_ISDA`, `ACT_ACT_ICMA`, `THIRTY_360_US`          |
| `notes`        | TEXT          |    | 例外や出典のメモ。  |
| `enabled`      | INTEGER       | Y  | 有効/無効の論理フラグ。   |
| `created_at`   | TEXT          | Y  | 監査用タイムスタンプ（UTC）。   |
##### 使用目的
- ディスカウントファクターやクーポンなどの算出に使用する。
- システム内で日数計算方式が必要な場合はこのテーブルから取得する。
---
#### 1.4 `bizday_convention`（営業日規則）
##### 説明（テーブル）
非営業日をどう繰り延べるかのルール。期日生成・満期調整に使用。
| 列名                 | 型            | 必須 | 説明              |
| ------------------ | ------------ | -- | ------------------------- |
| `code`               | TEXT (PK)    | Y  | 例：`F`, `MF`, `P`。外部指定キー。<br>用途: 取引やスケジュール生成で指定する実運用キー。  |
| `display_name`       | TEXT         | Y  | 画面表示名。例: `Following`, `Preceding`, `Modified Following`, `Modified Preceding`, `Nearest`。                     |
| `rule_tag`           | TEXT (CHECK) | Y  | 実装の分岐。期日調整関数で直接使用。<br>例:`FOLLOWING`, `PRECEDING`,`MOD_FOLLOWING`（同月維持。次月に入るならPRECEDINGへ）, `MOD_PRECEDING`（同月維持。前月に入るならFOLLOWINGへ）, `NEAREST`（前後の近い営業日へ）, `NONE`（無調整）        |
| `nearest_tiebreaker` | TEXT (CHECK) |    | `rule_tag`：`NEAREST`専用。前後が同距離のときどちらを選ぶかの規則。デフォルト`NULL`＝実装既定。 |
| `notes`              | TEXT         |    | 規約メモ。                     |
| `enabled`            | INTEGER      | Y  | 有効/無効。廃止時に物理削除せず切替。   |
| `created_at`         | TEXT         | Y  | 監査用UTCタイムスタンプ。    |
##### 使用目的
- キャッシュフロー日付などのスケジュール生成時の休日調整で使用する規則はこのテーブルから取得する。
---
#### 1.5 `calendar_def`（市場カレンダー定義）
##### 説明（テーブル）
市場ごとのタイムゾーンと「休日の集合」を紐づけるのに使用。
| 列名           | 型         | 必須 | 説明           |
| ------------ | --------- | -- | ------------------------- |
| `cal_id`       | TEXT (PK) | Y  | カレンダーID。例: `JPTO`, `USNY`。`currency_calendar`等の参照先。  |
| `display_name` | TEXT      | Y  | 表示名。例: `“Tokyo Market Calendar”`。  |
| `time_zone`    | TEXT      | Y  | IANA TZ。評価起算日のローカル日決定に使用。<br>例: `Asia/Tokyo`, `America/New_York`。 |
| `enabled`      | INTEGER   | Y  | 有効/無効フラグ。            |
| `created_at`   | TEXT      | Y  | 監査用UTC。             |
##### 使用目的
- 市場のタイムゾーンとカレンダー（休日集合）を紐づける。
---
#### 1.6 `market_holiday`（休場日・半休）
##### 説明（テーブル）
市場ごとの休場日と短縮取引日を保持。スケジュール生成・締時刻で使用。
| 列名           | 型                      | 必須 | 説明          |
| ------------ | ---------------------- | -- | --------------------- |
| `cal_id`       | TEXT (FK→calendar_def.cal_id) | Y  | カレンダーID。                 |
| `on_date`      | TEXT                   | Y  | 休日・半休の日付。`YYYY-MM-DD`。         |
| `holiday_name` | TEXT                   |    | 名称。例: `“元日”`, `“Labor Day (Observed)”`。   |
| `holiday_type` | TEXT                   |    | 例: `NATIONAL`, `BANK`, `MARKET`, `OBSERVED`, `AD_HOC`。調整ロジックの例外判定に使用可。 |
| `observed_of`  | TEXT                   |    | 振替元の日付（`YYYY-MM-DD`）。振替休日を精密再現。 |
| `is_half_day`  | INTEGER                | Y  | 半休フラグ。FIXやカットの締め時刻を将来扱う際の拡張点。例：半休=`1`。   |
| `notes`        | TEXT                   |    | 補足（臨時休場メモ等）。      |
| `enabled`      | INTEGER                | Y  | 有効/無効フラグ。      |
| `created_at`   | TEXT                   | Y  | 監査用UTC。            |
##### 使用目的
- スケジュール生成などで使用する休日はこのテーブルから取得する。
---
#### 1.7 `ref_rate_rule`（参照金利規則）
##### 説明（テーブル）
変動レグのFixing規約とO/N→期間化を定義。スワップやCap/Floor生成に必須。
| 列名                   | 型                  | 必須 | 説明             |
| -------------------- | ------------------ | -- | --------------------------------- |
| `ref_rate_id`          | TEXT (PK)          | Y  | 例：`'USD-SOFR'`,`'USD-SOFR-3M'`,`'JPY-TONAR'`。プログラムでレート情報と紐づけるためのキー。   |
| `ccy`                  | TEXT (FK→`currency.ccy`) | Y  | レートに対応する通貨。              |
| `tenor`                | TEXT               | Y  | レートのテナー。例：`'ON'`,`'1M'`,`'3M'`  |
| `daycount`             | TEXT (FK→`daycount.code`) | Y  | レートに対応する日数計算方式。             |
| `display_name`         | TEXT               | Y  | 表示名。UI・ログで誤認を避ける。例: `“USD SOFR 3M Term”`。   |
| `index_family`         | TEXT               | Y  | 系統ラベル。例: `SOFR`, `TONAR`, `LIBOR`。監査や検索で便利。 |
| `rate_type`            | TEXT (CHECK)       | Y  | O/N参照か期間物か。`'ON'`=翌日物（SOFR, TONAR）、`'TERM'`=1M/3M等のタームレート。レグの生成で分岐。    |
| `fixing_cal_id`        | TEXT (FK→`calendar_def.cal_id`)  | Y  | Fixing判定に使う市場カレンダーID。例: `USNY`, `JPTO`。指数の「観測日」はこのカレンダーで決まる。 |
| `fixing_bdc`           | TEXT (FK→`bizday_convention.code`)   | Y  | Fixing日の営業日調整（`F`/`MF`等）。指数の「観測日繰上げ/繰下げ」を明示。   |
| `fixing_time_local`    | TEXT               |    | ローカル時刻の締切。将来の締切ロジックや半休対応の拡張点（v1は保存のみ）。例: `'08:00'`。      |
| `fixing_tz`            | TEXT               |    | Fixingローカル日の決定。デフォルトのタイムゾーンは`calendar_def.time_zone`を使用する。実装では`COALESCE(ref_rate_rule.fixing_tz, calendar_def.time_zone)`として取得するイメージ。                   |
| `publication_lag_days` | INTEGER            | Y  | 公表遅延。SOFRは通常T+1など。EOD計測やバッチの依存順序に使用。    |
| `accrual_conv`         | TEXT (CHECK)       | Y  | O/Nを期間化する計算方法。<br>`SIMPLE`: 日割単利合成<br>`COMPOUND_IN_ARREARS`: 後決め複利（OISクーポン）<br>`AVERAGE`: 単純平均（公表平均を使う場合）<br>`TERM_QUOTE`: 期間物タームレート（1M/3M等）をそのまま使用 |
| `lookback_days`        | INTEGER            | Y  | 後決めでのルックバック（例: 5D lookback=`5`）。将来のOIS詳細に備える。v1は`0`。  |
| `lockout_days`         | INTEGER            | Y  | ロックアウト日数（期末数日を一定レート固定）。v1は`0`。 |
| `source_tag`           | TEXT               |    | データ取得元ベンダー。例：`bbg`, `reuters`, `manual` |
| `enabled`              | INTEGER            | Y  | 有効/無効。廃止時に物理削除せず切替。     |
| `valid_from`           | TEXT               | Y  | マスタ適用開始日。           |
| `retired_at`           | TEXT               |    | 廃止日。              |
| `created_at`           | TEXT               | Y  | 監査UTC。               |
##### 使用目的
- システム内で評価等に使用する参照金利レートに関する市場慣行やルールなどはこのテーブルから取得する。
---
#### 2.1 `market_snapshot`（評価時の入力値のスナップショット）
##### 説明（テーブル）
評価で参照するマーケット入力の“版”を一意に識別する。取込ソース、ローダのバージョン、データ同一性ハッシュ、承認・凍結情報を保持し、再現性と監査を担保する。
| 列名                   | 型                                           |  必須 | 説明（使用場面）                                      |
| -------------------- | ------------------------------------------- | :-: | --------------------------------------------- |
| `snapshot_id`        | TEXT（PK）                                    |  Y  | 版ID。全マーケット表や `run.snapshot_id` の参照先。          |
| `as_of`              | TEXT（`YYYY-MM-DD`）                          |  Y  | 論理市場日。EOD/カット選別の主キー軸。                         |
| `as_of_tz`           | TEXT（IANA TZ）                               |  Y  | 市場日の解釈TZ。例：`Asia/Tokyo`。                      |
| `cut_label`          | TEXT                                        |     | 運用カット名。`'EOD'`, `'NY_10AM'`, `'LDN_4PM'` など。  |              |
| `data_hash`          | TEXT（UNIQUE）                                |  Y  | 入力一式の総ハッシュ。重複取込防止と完全同一性確認。                    |
| `parent_snapshot_id` | TEXT（FK→`market_snapshot.snapshot_id`）                  |     | 親版。差分・派生の系譜管理。                                |
| `is_locked`          | INTEGER（`0`/`1`）                                |  Y  | `1`=ロック済。評価参照はロックのみを原則化。                        |
| `qa_status`          | TEXT（CHECK：`PENDING`/`APPROVED`/`REJECTED`） |     | 品質審査状態。本番利用ガードに使用。                            |                             |
| `note`               | TEXT                                        |     | 補足（臨時休場・欠測補完、qa_status：REJECTの場合の理由など）。                              |
| `frozen_at`    | TEXT（UTC）                                   |     | 版を凍結した実瞬間。最新確定選定に利用。                          |
| `created_at`         | TEXT（UTC）                                   |  Y  | 生成時刻。監査用。                                     |                                   |
##### 使用目的
- システム内で評価時に使用するマーケットはこのテーブルのレコードに紐づく。
- このテーブルのsnapshot_idに紐づくほかのマーケットデータ（テーブル）のレートを使用して計算する。
---
#### 2.2 `fx_spot`（為替スポットレート）
##### 説明（テーブル）
スナップショット時点の為替スポットを保持する。
評価の換算（PV通貨変換）、CIP（カバード金利平価）計算、将来の約定コスト見積（bid/ask）やクロス導出の監査に用いる。snapshot_id ごとに不変。
| 列名  | 型  |  必須 | 説明    |
| ----- | --- | --- | ---------------- | 
| `snapshot_id`      | TEXT（FK→`market_snapshot.snapshot_id`） |  Y  | どの版のレートかを識別。評価では `run.snapshot_id` と一致させて再現性を担保。                       |
| `base_ccy`         | TEXT（FK→`currency.ccy`）        |  Y  | レートの分子通貨。集計・JOINの主キー軸。例：`USD`。     |
| `quote_ccy`        | TEXT（FK→`currency.ccy`）        |  Y  | レートの分母通貨。例：`JPY`。  |
| `pair`             | TEXT                       |  Y  | ペア文字列。例：`USD/JPY`       |  
| `spot`             | REAL                       |  Y  | MIDレート。`bid/ask` がある場合は `(bid+ask)/2` をアプリでセット。PV換算やCIPの基準に用いる。    |
| `bid`              | REAL                       |     | 片サイド。`ask` と併用し約定コスト・スプレッド評価に使用。`bid <= ask` を CHECK。             |
| `ask`              | REAL                       |     | 片サイド。上記同様。      |
| `frozen_at`   | TEXT（UTC）                  |     | このペアの観測タイムスタンプ。同一スナップショット内での鮮度差や遅延解析に使用（`market_snapshot.frozen_at` は版の確定瞬間、こちらは銘柄個別）。 |
| `source_symbol`    | TEXT                       |     | ベンダの**ティッカー/銘柄ID**。障害時のトレースやベンダ仕様差の切り分けに使用。      |
| `is_cross_derived` | INTEGER（`0`/`1`）               |  Y  | `1`=クロス導出（例：`EURJPY = EURUSD × USDJPY`）。直物が無い通貨ペアの監査に有用。     |
| `derived_via_1`    | TEXT                       |     | クロス導出の由来ペア1。`is_cross_derived`=`1` のとき必須。   |
| `derived_via_2`    | TEXT                       |     | クロス導出の由来ペア2。`is_cross_derived`=`1` のとき必須。                 |
##### 使用目的
- 評価時に使用する為替レートはこのテーブルから取得する。
---
#### 2.3 `pricing_curve_def`（評価用カーブ定義）
##### 説明（テーブル）
評価で用いるイールドカーブの定義を管理する。割引（OIS）、予測（FORECAST）、債券（BOND）の区別に加え、daycount/compounding、補間/外挿、ピラーの表現方法、構築方法などを列で固定化し、評価ロジックをデータ駆動で再現できるようにする。
| 列名             | 型                          |  必須 | 説明        |
| ---------------- | --------------------------- | --- | ------------ |
| `curve_id`       | TEXT（PK）           |  Y  | カーブの一意名。ログやUIで参照。`curve_point.curve_id` の親。                 |
| `ccy`            | TEXT（FK→`currency.ccy`） |  Y  | カーブの通貨。`FORECAST` でも“レグの支払通貨”ではなくカーブの通貨を指す。        |
| `curve_type`     | TEXT（`OIS`,`FORECAST`,`BOND`）         |  Y  | 用途の区別。割引＝`OIS`、フォワード予測＝`FORECAST`、債券用＝`BOND`。評価ロジックの分岐キー。           |
| `ref_rate_id`    | TEXT          |     | `FORECAST` のみ必須。対象参照金利（例：`JPY-TONAR`,`USD-SOFR`）。スケジュール側の `ref_rate_rule` と結合して年率化τやFixing規約を取得する。`OIS/BOND` では `NULL`。 |
| `daycount`       | TEXT（FK→`daycount.code`）   |  Y  | ゼロ↔DF 変換で使用（クーポン年率化とは別物）。 |
| `compounding`    | TEXT（`CONT`,`SIMPLE`,`ANNUAL`）        |  Y  | DFの計算等に使用。   |
| `interp_method`  | TEXT（FK→`m_interp_method.interp_method`）|  Y  | ピラー間の補間アルゴリズム。例：`LINEAR`, `LOG_LINEAR`, `NATURAL_CUBIC_SPLINE`など。 |
| `extrap_left`    | TEXT（列挙）                              |  Y  | 左外挿の規約。`FLAT_FWD`（フォワード一定）、`FLAT_ZERO`、`LINEAR_ZERO` 等。   |
| `extrap_right`   | TEXT（列挙）                              |  Y  | 右外挿の規約。長期端のPV感応に影響するため明示列で固定。      |
| `pillar_mode`    | TEXT（`TENOR`,`DATE`,`MIXED`）          |  Y  | ピラーが「テナー」か「実日付」かの定義。`curve_point.tenor` と `pillar_date` の使い分けに関与。   |
| `build_method`   | TEXT（`RAW_DF`,`RAW_ZERO`,`BOOTSTRAP`） |  Y  | 入力の由来。`RAW_*` はベンダの値をそのまま採用。`BOOTSTRAP` はブートストラップ。    |
| `cal_id`         | TEXT（FK→`calendar_def.cal_id`）  |     | ピラー日付生成の営業日調整などに使用。通常は NULL。       |
| `collateral_ccy` | TEXT（FK→`currency`）                   |     | 担保通貨を明示したい場合に使用（OIS割引の担保条件と合わせたい場合など）。未使用なら NULL。     |
| `valid_from`     | TEXT                                  |  Y  | この定義の適用開始日（論理）。将来の仕様改訂に備える。      |
| `valid_to`       | TEXT           |     | 適用終了日。NULL は現行。履歴管理に使用。       |
| `description`    | TEXT               |     | 自由記述。運用メモ、注意点など。      |
##### 使用目的
- 評価時に使用するイールドカーブの補間方法や日数計算方法などのメタ情報が定義されている。
---
#### 2.4 `curve_point`（カーブの値）
##### 説明（テーブル）
スナップショット時点でのカーブのピラー値を保持する。pricing_curve_def の規約（daycount, compounding, interp/extrap）に従い、補間・外挿の入力として使う。座標 x_years を明示して計算を安定化し、値は DF または 連続複利ゼロで保持する（両方があれば相互検証可）。
| 列名                | 型                                            |  必須 | 説明     |
| ----------------- | -------------------------------------------- | :-: | ------------------------------------------------------------------------ |
| `snapshot_id`     | TEXT（FK→`market_snapshot.snapshot_id`）  |  Y  | 版の識別子。評価は `run.snapshot_id` と一致させ再現性を担保。                                 |
| `curve_id`        | TEXT（FK→`pricing_curve_def.curve_id`）    |  Y  | どの定義のカーブか。`curve_type` や `daycount` 等は親の定義に従う。                           |
| `tenor`           | TEXT                                         |     | ピラーをテナーで表す場合のキー。例：`'1M'`。`pillar_mode='TENOR'` で主に使用。                    |
| `pillar_date`     | TEXT                                         |     | ピラーを実日付で表す場合のキー。例：`'2027-09-15'`。`pillar_mode='DATE'` で主に使用。             |
| `x_years`         | REAL                                 |  Y  | 座標の年率時間。`pricing_curve_def.daycount` に基づき `as_of` から算出。補間・外挿は基本的にこの値で実施。 |
| `df`              | REAL                                         |     | ディスカウントファクター。`interp_method='LOG_LINEAR_DF'` の入力として自然。                |
| `zero_cont`       | REAL                                         |     | 連続複利ゼロ (r)（年率）。`DF=\exp(-r\cdot x\_{years})` の関係。`df` とどちらか必須。           |
| `is_extrapolated` | INTEGER（0/1）                                 |  Y  | 1=外挿点。ピラー外に生成した点を識別し、感応や監査で注意喚起。                                         |
| `pillar_source`   | TEXT（`RAW`,`BOOTSTRAP`,`DERIVED`,`SMOOTHED`） |  Y  | 値の由来。RAW=生値、BOOTSTRAP=積上げ推定、DERIVED=式から導出、SMOOTHED=平滑化後。                 |
| `source_symbol`   | TEXT                                         |     | ベンダ銘柄/系列の識別。障害切り分けや再取得で使用。                                               |
| `created_at`      | TEXT（UTC）                                    |  Y  | 取込UTC。監査列。               |
##### 使用目的
- 評価時に使用するイールドカーブのレートの値はこのテーブルから取得する。
- このテーブルの値を使って補間処理を行い、評価に使用する。
---　
#### 2.5 `vol_fx`（為替ボラティリティ）
##### 説明（テーブル）
スナップショット時点のFX Implied Volatility Surfaceを保持する。
満期（時間軸）とスマイル軸（ATM / DELTA / STRIKE）の指定を正規化し、Garman–Kohlhagen（Black）または Bachelierでのプレミアム計算、リスク指標（vega など）、面内補間・外挿をデータ駆動で再現可能にする。
| 列名               | 型                            |  必須 | 説明（使用場面）    |
| ---------------- | ---------------------------- | :-: | ------ |
| `vol_id`         | TEXT（PK）                     |  Y  | 1行1点の一意ID（UUIDなど）。複雑な複合キーを避ける。   |
| `snapshot_id`    | TEXT（FK→`market_snapshot.snapshot_id`）   |  Y  | 版。`run.snapshot_id` と一致させ再現性を担保。     |
| `base_ccy`       | TEXT（FK→`currency.ccy`）          |  Y  | 分子通貨。         |
| `quote_ccy`      | TEXT（FK→`currency.ccy`）          |  Y  | 分母通貨。         |
| `pair`           | TEXT                         |  Y  | 'USDJPY'。 |
| `expiry_tenor`   | TEXT                         |     | 満期テナー（'1W','1M','1Y'）。         |
| `expiry_date`    | TEXT                         |     | 満期実日付。テナー／日付のどちらでも管理可能。      |
| `x_years`        | REAL                         |  Y  | **ボラ時間**（年率時間）。補間・外挿・vega計算の独立変数。 |
| `vol_daycount`   | TEXT（FK→`daycount.code`）          |  Y  | 時間の年率化規約（FXは ACT/365F が一般的）。|
| `smile_type`     | TEXT（'ATM','DELTA','STRIKE'） |  Y  | スマイル軸の指定方式。以下の各条件と排他的。     |
| `atm_type`       | TEXT（'ATM_DN','ATM_FWD'）     |     | `smile_type='ATM'` のとき必須。ATMの定義差を明示。   |
| `delta_type`     | TEXT（'SPOT','FWD'）           |     | `smile_type='DELTA'` のとき必須。デルタの定義。    |
| `premium_adj`    | INTEGER（0/1）                 |     | `smile_type='DELTA'` のとき必須。1=PA, 0=NPA。  |
| `option_type`    | TEXT（'C','P'）                |     | `smile_type='DELTA'` のとき必須。    |
| `delta_abs`      | REAL                         |     | `smile_type='DELTA'` のとき必須。例：0.25 は 25D。   |
| `strike`         | REAL                         |     | `smile_type='STRIKE'` のとき必須。行使価格（pairの価格単位）。  |
| `quote_type`     | TEXT（'LN_VOL','N_VOL'）       |  Y  | Black か Bachelier か。    |
| `sigma`          | REAL                         |  Y  | インプライド・ボラティリティ。      |
| `quote_time_utc` | TEXT                         |     | 当点の観測UTC（`market_snapshot.freeze_time_utc` と併用して鮮度を判断）。  |
| `source_symbol`  | TEXT                         |     | ベンダ銘柄/ティッカー。障害切り分け用。   |
| `surface_tag`    | TEXT                         |     | 面の用途タグ（'TRADABLE','CALIB','VENDOR_A' 等）。  |
| `created_at`     | TEXT（UTC）                    |  Y  | 取込UTC。監査用。               |
##### 使用目的
- 評価時に使用する為替ボラティリティはこのテーブルから取得する。
---
#### 2.6 `vol_capfloor`（Cap/Floorボラティリティ）
##### 説明（テーブル）
Cap/Floor のインプライド・ボラ面。時間軸（満期）×インデックス・テナー（3M/6M 等）×スマイル（ATM/STRIKE）を保持。Black/Bachelier でのプレミアム計算、vega 等の感応度、面内補間をデータ駆動で再現。
| 列名               | 型                          |  必須 | 説明（使用場面）                              |
| ---------------- | -------------------------- | :-: | ------------------------------------- |
| `vol_id`         | TEXT（PK）                   |  Y  | 一意ID。複合キーの煩雑さ回避。                      |
| `snapshot_id`    | TEXT（FK→`market_snapshot.snapshot_id`） |  Y  | 版の識別。評価の再現性。          |
| `ccy`            | TEXT（FK→`currency.ccy`）        |  Y  | 通貨。                              |
| `ref_rate_id`    | TEXT                      |     | 参照金利（例：`JPY-TONAR`）。指数の規約と連携したい場合に使用。|
| `index_tenor`    | TEXT                       |  Y  | インデックスの期間（'3M' など）。                   |
| `expiry_tenor`   | TEXT                       |     | 満期テナー。                                |
| `expiry_date`    | TEXT                       |     | 満期実日付。                                |
| `x_years`        | REAL                       |  Y  | 年率時間 (T)。補間・価格式の独立変数。                 |
| `vol_daycount`   | TEXT（FK→`daycount.code`）   |  Y  | ボラ時間の年率化規約（一般に ACT/365F）。     |
| `smile_type`     | TEXT（'ATM','STRIKE'）       |  Y  | スマイル軸の指定。                             |
| `strike_rate`    | REAL                       |     | `STRIKE` のとき必須。行使率（実数）。               |
| `quote_type`     | TEXT（'LN_VOL','N_VOL'）     |  Y  | Black or Bachelier。                   |
| `sigma`          | REAL                       |  Y  | インプライド・ボラ。                            |
| `quote_time_utc` | TEXT                       |     | 観測UTC。                                |
| `source_symbol`  | TEXT                       |     | ベンダ識別。                                |
| `surface_tag`    | TEXT                       |     | 面の用途（'TRADABLE','CALIB' 等）。           |
| `created_at`     | TEXT（UTC）                  |  Y  | 取込UTC。                                |
##### 使用目的
- 評価時に使用するCap/Floorボラティリティはこのテーブルから取得する。
---
#### 2.7 `vol_swaption`（Swaptionボラティリティ）
##### 説明（テーブル）
Swaption のATM ボラ面（満期×スワップ年限）。Black/Bachelier の校正やプレミアム計算、vega、面内補間を再現。
| 列名               |                      型 |  必須 | 説明               |
| ---------------- | ---------------------: | :-: | ---------------- |
| `vol_id`         |               TEXT（PK） |  Y  | 一意ID。            |
| `snapshot_id`    |               TEXT（FK） |  Y  | 版。               |
| `ccy`            |               TEXT（FK） |  Y  | 通貨。              |
| `expiry_tenor`   |                   TEXT |     | 満期テナー。           |
| `expiry_date`    |                   TEXT |     | 満期実日付。           |
| `swap_tenor`     |                   TEXT |  Y  | 原資産スワップ期間。        |
| `x_years`        |                   REAL |  Y  | 年率時間（オプション満期）。   |
| `vol_daycount`   |               TEXT（FK） |  Y  | ボラ時間の年率化規約。      |
| `quote_type`     | TEXT（'LN_VOL','N_VOL'） |  Y  | Black/Bachelier。 |
| `sigma`          |                   REAL |  Y  | ATM ボラ。          |
| `quote_time_utc` |                   TEXT |     | 観測UTC。           |
| `source_symbol`  |                   TEXT |     | ベンダ識別。           |
| `surface_tag`    |                   TEXT |     | 面の用途タグ。          |
| `created_at`     |              TEXT（UTC） |  Y  | 取込UTC。           |
##### 使用目的
- 評価時に使用するSwaptionボラティリティはこのテーブルから取得する。
---
#### 2.8 `model_param`（モデルパラメータ）
##### 説明（テーブル）
スナップショット別のモデル固定値／上書き。ボラ面の shift、SABR の 𝛼,𝛽,𝜌,𝜈, Bachelier の基準拡散等を保持。
| 列名              | 型         |  必須 | 説明                                     |
| --------------- | --------- | :-: | -------------------------------------- |
| `snapshot_id`   | TEXT（FK）  |  Y  | 版。                                     |
| `model_tag`     | TEXT      |  Y  | モデル識別（'BLACK','BLACK_SHIFT','SABR' 等）。 |
| `scope`         | TEXT      |  Y  | 適用範囲（'CCY','PAIR','INDEX','GLOBAL'）。   |
| `param_key`     | TEXT      |  Y  | 適用キー（'JPY','USDJPY','JPY-TONAR' 等）。    |
| `param_name`    | TEXT      |  Y  | パラメータ名（'shift','alpha','beta' 等）。      |
| `param_val`     | REAL      |  Y  | 値。                                     |
| `param_unit`    | TEXT      |     | 単位（'abs','bp','ratio' 等）。              |
| `source_symbol` | TEXT      |     | ベンダ/由来。                                |
| `note`          | TEXT      |     | 備考。                                    |
| `created_at`    | TEXT（UTC） |  Y  | 取込UTC。                                 |
##### 使用目的
- 評価時に使用する各種モデルのパラメータはこのテーブルから取得する。
---
#### 3.1 `book`（ブック）
##### 説明（テーブル）
集計・報告の単位。小規模でも Desk/帳簿ごとに結果を束ねる。
| 列名            | 型        |  必須 | 説明          |
| ------------- | -------- | :-: | ----------- |
| `book_id`     | TEXT（PK） |  Y  | 帳簿ID。       |
| `description` | TEXT     |     | 説明。         |
| `owner`       | TEXT     |     | 責任者/Desk 名。 |
| `created_at`  | TEXT     |  Y  | 作成UTC。      |
---
#### 3.2 `trade`（取引明細）
##### 説明（テーブル）
取引本体。商品固有は JSON に格納し、一般キーは列で保持。後続の評価は product でルーティングし、json_body から規約を解決。
| 列名               | 型             |  必須 | 説明                     |
| ---------------- | ------------- | :-: | ---------------------- |
| `trade_id`       | TEXT（PK）      |  Y  | 取引ID。                  |
| `product`        | TEXT          |  Y  | 商品種別（CHECK 列挙）。        |
| `book_id`        | TEXT（FK）      |     | 帳簿。                    |
| `ccy`            | TEXT（FK）      |  Y  | 報告/換算通貨。               |
| `notional`       | REAL          |  Y  | 元本。                    |
| `direction`     | INTEGER（CHECK） |  Y  | 単レグ商品の受払い符号（-1/1）。スワップ等はサブ側の `pay_rec` が主体。|
| `trade_date`     | TEXT           |  Y  | 取引日。  |
| `effective_date`  | TEXT           |     | 発効/開始。  |
| `maturity_date`   | TEXT           |     | 満期/最終支払日。                   |
| `status`          | TEXT（CHECK）    |     | `NEW`,`AMENDED`,`CANCELLED`,`EXERCISED`,`EXPIRED`,`NOVATED`。|
| `version_no`      | INTEGER        |     | 更改版。                   |
| `parent_trade_id`      | TEXT（FK）       |     | 親取引。              |
| `replaced_by_trade_id` | TEXT（FK）       |     | 後継取引。            |
| `is_active`            | INTEGER        |  Y  | 1=有効。           |
| `external_id`          | TEXT           |     | 外部一意キー。          |
| `counterparty`         | TEXT           |     | 相手先（CVA・ネットティング）。    |
| `cleared_flag`         | INTEGER        |     | CCP清算（0/1）。      |
| `clearing_house`       | TEXT           |     | JSCC/LCH 等。         |
| `csa_id`               | TEXT           |     | CSA ID。          |
| `netting_set_id`       | TEXT           |     | ネッティング集合。        |
| `pricing_profile_id`   | TEXT           |     | 評価規約セット ID。         |
| `trader`               | TEXT           |     | トレーダ。            |
| `sales`                | TEXT           |     | セールス。         |
| `strategy_tag`         | TEXT           |     | HEDGE/ALM/CLIENT_FLOW 等。       |
| `valid_from`           | TEXT           |  Y  | 適用開始。         |
| `created_at`           | TEXT           |  Y  | 生成UTC。       |
| `updated_at`           | TEXT           |     | 最終更新UTC。         |
| `closed_at`            | TEXT           |     | 経済クローズUTC。        |
| `canceled_at`          | TEXT           |     | 取消UTC。         |
| `source_tag`           | TEXT           |     | 由来。          |
##### 使用目的
- すべての取引が格納されているため、全体の商品管理に使用する。
---
#### 3.3 `trade_irs`（金利スワップ）
##### 説明（テーブル）
金利スワップの取引管理。
| 列名                | 型           |  必須 | 説明                          |
| ----------------- | ----------- | :-: | --------------------------- |
| `trade_id`        | TEXT（PK/FK） |  Y  | ヘッダリンク。                     |
| `pay_rec`         | TEXT（CHECK） |  Y  | 固定側 `'PAY'`/`'REC'`。        |
| `fixed_rate`      | REAL        |  Y  | 固定クーポン年率。                   |
| `fixed_daycount`  | TEXT（FK）    |  Y  | 固定年率化。                      |
| `fixed_freq`      | TEXT        |  Y  | 固定支払頻度。                     |
| `fixed_bdc`       | TEXT（FK）    |  Y  | 固定BDC。                      |
| `fixed_cal_id`    | TEXT（FK）    |  Y  | 固定カレンダー。                    |
| `float_index_id`  | TEXT（FK）    |  Y  | 参照金利。                       |
| `float_spread`    | REAL        |  Y  | 変動スプレッド。                    |
| `float_daycount`  | TEXT（FK）    |  Y  | 変動年率化。                      |
| `float_freq`      | TEXT        |  Y  | 変動支払頻度。                     |
| `float_bdc`       | TEXT（FK）    |  Y  | 変動BDC。                      |
| `float_cal_id`    | TEXT（FK）    |  Y  | 変動カレンダー。                    |
| `stub_type`       | TEXT        |     | `'FRONT'/'BACK'/'BOTH'` など。 |
| `amortizing_json` | TEXT        |     | 将来のノッチ・アモタイズ規約。             |
| `settle_ccy`      | TEXT（FK）    |     | 決済通貨。                       |
---
#### 3.4 `trade_bond`（債券）
##### 説明（テーブル）
債券の取引管理。
| 列名                | 型           |  必須 | 説明                        |
| ----------------- | ----------- | :-: | ------------------------- |
| `trade_id`        | TEXT（PK/FK） |  Y  | ヘッダリンク。                   |
| `coupon_type`     | TEXT（CHECK） |  Y  | `'FIX'`/`'FLOAT'`/`'ZC'`。 |
| `coupon_rate`     | REAL        |  条件 | `FIX` のとき必須。年率。           |
| `coupon_daycount` | TEXT（FK）    |  条件 | `FIX`/`FLOAT` のとき必須。      |
| `coupon_freq`     | TEXT        |  条件 | `FIX`/`FLOAT` のとき必須。      |
| `coupon_bdc`      | TEXT（FK）    |  条件 | `FIX`/`FLOAT` のとき必須。      |
| `coupon_cal_id`   | TEXT（FK）    |  条件 | `FIX`/`FLOAT` のとき必須。      |
| `float_index_id`  | TEXT（FK）    |  条件 | `FLOAT` のとき必須。            |
| `float_spread`    | REAL        |  条件 | `FLOAT` のとき必須。            |
| `issuer`          | TEXT        |     | 発行体。                      |
| `redemption`      | REAL        |  Y  | 額面償還（通常 100）。             |
| `settlement_ccy`  | TEXT（FK）    |  Y  | 決済通貨。                     |
---
#### 3.5 `trade_fxfwd`（FXフォワード）
##### 説明（テーブル）
FXフォワードの取引管理。
| 列名                                | 型             |  必須 | 説明                      |
| --------------------------------- | ------------- | :-: | ----------------------- |
| `trade_id`                        | TEXT（PK/FK）   |  Y  |                         |
| `base_ccy` / `quote_ccy` / `pair` | TEXT（FK/テキスト） |  Y  | 通貨ペア。                   |
| `deliver_date`                    | TEXT          |  Y  | 受渡日。                    |
| `forward_rate`                    | REAL          |  Y  | 契約レート。                  |
| `settle_bdc`                      | TEXT（FK）      |  Y  |                         |
| `deliver_cal_id`                  | TEXT（FK）      |  Y  |                         |
| `pay_rec_base`                    | TEXT（CHECK）   |  Y  | ベース通貨を `'PAY'`/`'REC'`。 |
---
#### 3.6 `trade_fxopt`（FXオプション）
##### 説明（テーブル）
FXオプションの取引管理。
| 列名                                   | 型             |  必須 | 説明                                  |
| ------------------------------------ | ------------- | :-: | ----------------------------------- |
| `trade_id`                           | TEXT（PK/FK）   |  Y  |                                     |
| `base_ccy` / `quote_ccy` / `pair`    | TEXT（FK/テキスト） |  Y  |                                     |
| `cp_flag`                            | TEXT（CHECK）   |  Y  | `'C'`/`'P'`。                        |
| `option_style`                       | TEXT（CHECK）   |  Y  | `'EUROPEAN'/'AMERICAN'/'BERMUDAN'`。 |
| `strike`                             | REAL          |  Y  |                                     |
| `expiry_date`                        | TEXT          |  Y  |                                     |
| `settlement`                         | TEXT（CHECK）   |  Y  | `'PHYS'`/`'CASH'`。                  |
| `premium_ccy` / `premium_amount`     | TEXT（FK）/REAL |     | プレミアム。                              |
| `deliver_cal_id` / `exercise_cal_id` | TEXT（FK）      |     |                                     |
| `barrier_type`                       | TEXT（CHECK）   |     | `'KI'`/`'KO'`。                      |
| `barrier_dir`                        | TEXT（CHECK）   |     | `'UP'`/`'DOWN'`。                    |
| `barrier_level`                      | REAL          |     |                                     |
| `rebate_type`                        | TEXT（CHECK）   |     | `'AT_HIT'`/`'AT_EXPIRY'`。           |
| `rebate_ccy` / `rebate_amount`       | TEXT（FK）/REAL |     | リベート。                               |
| `monitoring_cal_id`                  | TEXT（FK）      |     | バリア監視日カレンダー。                        |
---
#### 3.7 `trade_capfloor`（Cap/Floor）
##### 説明（テーブル）
Cap/Floorの取引管理。
| 列名                        | 型           |  必須 | 説明                     |
| ------------------------- | ----------- | :-: | ---------------------- |
| `trade_id`                | TEXT（PK/FK） |  Y  |                        |
| `ccy`                     | TEXT（FK）    |  Y  |                        |
| `cp_flag`                 | TEXT（CHECK） |  Y  | `'C'`=Cap、`'P'`=Floor。 |
| `index_id`                | TEXT（FK）    |  Y  |                        |
| `index_tenor`             | TEXT        |  Y  |                        |
| `strike_rate`             | REAL        |  Y  |                        |
| `start_date` / `end_date` | TEXT        |  Y  |                        |
| `pay_rec`                 | TEXT（CHECK） |  Y  | `'PAY'`/`'REC'`。       |
| `pay_freq`                | TEXT        |  Y  |                        |
| `pay_bdc`                 | TEXT（FK）    |  Y  |                        |
| `pay_cal_id`              | TEXT（FK）    |  Y  |                        |
---
#### 3.8 `trade_swaption`（Swaption）
##### 説明（テーブル）
Swaptionの取引管理。
| 列名    | 型               |  必須 | 説明                                  |
| ------- | --------------- | :-: | ----------------------------------- |
|`trade_id`    | TEXT（PK/FK）     |  Y  |                                     |
| `ccy`         | TEXT（FK）        |  Y  |                                     |
| `option_style`| TEXT（CHECK）     |  Y  | `'EUROPEAN'/'AMERICAN'/'BERMUDAN'`。 |
| `cp_flag`     | TEXT（CHECK）     |  Y  | `'C'`（payer）/`'P'`（receiver）。       |
| `expiry_date` | TEXT            |  Y  | EU：権利行使日、AM/BER：最終行使期限。             |
| `exercise_open` / `exercise_close`| TEXT            |  条件 | AM のとき必須（ウィンドウ）。   |
| `bermudan_dates_json` | TEXT            |  条件 | BER のとき必須（行使可能日配列）。        |
| `settlement`  | TEXT（CHECK）     |  Y  | `'PHYS'`/`'CASH'`。                  |
| `swap_pay_rec`| TEXT（CHECK）     |  Y  | 基底スワップ固定側の `'PAY'`/`'REC'`。         |
| `swap_fixed_rate`| REAL            |     | ATMF なら NULL。                       |
| `swap_fixed_dc` / `swap_fixed_freq` / `swap_fixed_bdc` / `swap_fixed_cal` | TEXT（FK含む）|  Y  | 固定レグ規約。|
| `swap_index_id` / `swap_index_tenor` / `swap_spread` / `swap_float_dc` / `swap_float_freq` / `swap_float_bdc` / `swap_float_cal` | TEXT/REAL（FK含む） |  Y  | 変動レグ規約。                             |
| `swap_maturity` | TEXT            |  Y  | 基底スワップ満期。                           |
---
#### 4.1 `run`（評価実行管理）
##### 説明（テーブル）
評価実行のメタ。どの版（snapshot_id）で、どのコード（code_hash）を誰が（runner）走らせたか。
| 列名                | 型        |  必須 | 説明                |
| ----------------- | -------- | :-: | ----------------- |
| `run_id`          | TEXT（PK） |  Y  | 実行ID。             |
| `as_of`           | TEXT     |  Y  | 論理市場日。            |
| `snapshot_id`     | TEXT（FK） |  Y  | 参照スナップショット。       |
| `scenario_set_id` | TEXT     |     | シナリオセット。          |
| `code_hash`       | TEXT     |  Y  | 実行コードのハッシュ。       |
| `runner`          | TEXT     |     | 実行者/ホスト/CI ジョブ名等。 |
| `created_at`      | TEXT     |  Y  | 実行UTC。            |
---
#### 4.2 `result`（実行結果）
##### 説明（テーブル）
評価結果の格納。将来、感応度や分解に拡張可能。calc_time_ms で性能計測も可能。
| 列名             | 型        |  必須 | 説明             |
| -------------- | -------- | :-: | -------------- |
| `run_id`       | TEXT（FK） |  Y  | 実行。            |
| `trade_id`     | TEXT（FK） |  Y  | 取引。            |
| `measure`      | TEXT     |  Y  | 'PV' 等。        |
| `bucket`       | TEXT     |     | サブ粒度キー。        |
| `val`          | REAL     |  Y  | 値。             |
| `ccy`          | TEXT（FK） |  Y  | 結果通貨。          |
| `calc_time_ms` | REAL     |     | 1件あたり計算時間（ms）。 |
---
#### 5.1 `scenario_set`（シナリオ集合）
##### 説明（テーブル）
シナリオの集合。
| 列名                | 型        |  必須 | 説明     |
| ----------------- | -------- | :-: | ------ |
| `scenario_set_id` | TEXT（PK） |  Y  | セットID。 |
| `set_name`        | TEXT     |  Y  | 名称。    |
| `description`     | TEXT     |     | 説明。    |
| `created_at`      | TEXT     |  Y  | 作成UTC。 |
---
#### 5.2 `scenario_shock`（シナリオのショック）
##### 説明（テーブル）
シナリオ変動の定義
| 列名                | 型        |  必須 | 説明                                             |
| ----------------- | -------- | :-: | ---------------------------------------------- |
| `scenario_set_id` | TEXT（FK） |  Y  | 親セット。                                          |
| `scenario_id`     | INTEGER  |  Y  | 0,1,2…                                         |
| `target`          | TEXT（列挙） |  Y  | 'CURVE','FX','VOL','MODEL_PARAM'。              |
| `target_key`      | TEXT     |  Y  | 'JPY-OIS','USDJPY','vol_swaption:JPY' 等、対象のキー。 |
| `op_tag`          | TEXT（列挙） |  Y  | 'ADD_BP','MULT','SET'。                         |
| `shock_val`       | REAL     |  Y  | 値。                                             |
| `note`            | TEXT     |     | 任意メモ。                                          |
