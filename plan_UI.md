## UI 方針（Web アプリ）

### 1. UI 全体コンセプト

- 対象  
  - 個人利用・検証用途を主としつつ、小規模金融機関のフロント／ミドル業務でも日常的に使えるレベルを目指す。

- 目的  
  - 主な業務フロー（マーケット取込・確認 → マーケット版確定 → カーブ構築 → 取引登録 → 評価実行 → 結果確認／比較 → 出力）を、ブラウザ上で一貫して完結できるようにする。  
  - 「どのデータ・どの条件で計算したか」を常に説明可能にし、再現性とトレーサビリティを UI で担保する。

- デザイン方針
  - CSSは tailwind を使用する。
  - 見た目は「シンプルで整理されている」ことを最優先としつつ、余白・タイポグラフィ・色数を適度に使い、業務アプリとしてしょぼく見えない最低限の質感を確保する。  
  - 設定（ポートフォリオ・シナリオ集合・評価条件プリセット等）の変更は、できるだけ画面上から完結できるようにし、「コードを書き換えないと変えられない設定」を最小限に抑える。  
  - 対象は PC ブラウザのみとし、横幅のある画面での一覧表示や複数パネル配置を前提に設計する。  
  - 複雑な設定や詳細情報は、必要なときに展開できるよう折りたたみやタブで隠し、日々の運用に必要な最小限の情報を常に前面に出す。  
  - 「編集＝上書き」ではなく、原則として版管理（新しい版の作成）で運用できる UI を優先する（例：マーケット版、取引修正）。

---

### 2. 画面構成（グローバルなメニュー構造）

アプリ全体は、上部に固定されたメインメニューを持つ Web アプリとし、次の区分を基本とする。

1. ログイン  
2. 取引管理  
3. マーケット管理  
4. 評価・結果  
5. 設定・マスタ  
6. 管理（アドミニストレーター専用）

各区分に含める画面の粒度は以下とする（名称はあくまで方針レベル）。

- ログイン  
  - ユーザのログイン画面。

- 取引管理  
  - 取引一覧画面  
  - 取引の登録・編集画面（商品ごとの入力フォーム）  
  - 取引詳細画面（バージョン履歴、主要条件、スケジュール／キャッシュフロー参照、ライフサイクル操作の入口）

- マーケット管理  
  - マーケット取込（ファイル取込・再取込・失敗確認）  
  - 取込履歴（取込単位の一覧・詳細）  
  - クォート確認（異常値・前日差・採用／除外の操作）  
  - マーケット版の作成（コンポーズ：必要な入力の選択）  
  - マーケット版の一覧画面  
  - 個々のマーケット版の詳細画面（為替・金利・ボラティリティ等をタブで切り替え、承認／ロックの操作も含む）  
  - カーブ構築（対象版・対象カーブを指定して実行）  
  - カーブ確認（表形式での点検、必要に応じて前回との差分）  
  - Fixing 管理（未確定の Fixing の確認、確定入力、取込、承認）

- 評価・結果  
  - 評価ジョブの起動画面（評価条件の指定：マーケット版・対象範囲・指標・シナリオなど）  
  - 評価ジョブの履歴一覧画面  
  - 個々の評価ジョブの詳細画面（条件・対象範囲・実行状況・エラー詳細）  
  - 評価結果サマリ／明細画面（ポートフォリオ別・取引別の参照）  
  - 出力（画面条件でのエクスポート、定型レポート出力の入口）

- 設定・マスタ  
  - 日常的に変更する設定（ポートフォリオ、シナリオ集合、評価条件プリセット、評価プロファイル等）の簡易編集画面  
  - 指標定義（集計対象・感応度のバケットなど、運用上増えやすい定義）の参照／有効無効／簡易編集  
  - 基礎的な設定（通貨・日数計算・営業日規則・カレンダー等）は原則参照専用（v1）

- 管理（アドミニストレーター専用）  
  - ユーザ管理画面（ユーザの追加・削除・無効化、パスワードリセットなど）  
  - 権限管理画面（ユーザごとのロール設定： v1では 通常ユーザ／管理者 のみとする）  
  - 本メニューはアドミニストレーター権限でログインした場合のみ表示・アクセス可能とし、一般ユーザには一切見えない構成とする。

---

### 3. 代表画面ごとの方針

#### 3.1 ログイン

- 目的  
  - セキュリティのためログインしたユーザのみが参照・操作ができる。

- 主な表示内容  
  - ユーザID、パスワードの入力欄  
  - ログインボタン

- 方針  
  - パスワードはハッシュ化して保持する。  
  - パスワードのハッシュ化はソルト、ペッパー、ストレッチングを利用する。

---

#### 3.2 取引一覧画面

- 目的  
  - 登録済み取引を検索・参照し、必要に応じて詳細確認や修正（新バージョン作成）へ遷移する。

- 主な表示内容  
  - 取引ID、商品種別、ブック、通貨、名目、主要日付（取引日・開始日・満期）  
  - 状態（有効／取消／クローズ等）、最新版フラグ  
  - 必要に応じて「要 Fixing」「評価失敗」などの注意フラグ

- 方針  
  - 行数が多くなることを前提に、ページング・フィルタ（ブック／商品／通貨／状態）・並べ替えを導入する。  
  - 取引の主要条件は一覧で把握し、細かい条件（オプション詳細、スケジュール等）は取引詳細画面で確認する。  
  - エクスポート（CSV 等）は v1 から提供する。

---

#### 3.3 取引登録・編集画面（＋取引詳細）

- 目的  
  - 新規取引の登録、および既存取引の修正（新バージョンとしての登録）を行う。

- UI 方針  
  - 画面は「商品ごとに 1 つの編集画面」を基本とし、共通項目と商品固有項目を同一画面内で縦方向に配置し、「スクロールすれば一望できる」構成を目指す。  
  - 共通項目と商品固有項目は、画面上ではセクション見出し・枠で区切るにとどめ、画面遷移やウィザード形式では分割しない（戻る／進む操作が不要な体験を優先）。  
  - 通貨・ブック・カレンダー・インデックスなど、選択肢が限定されるものはプルダウンで選び、コードの直接入力を最小限にする。  
  - 必須項目かどうかを視覚的に明示し、入力漏れがある場合は画面上部または項目近くにエラーメッセージを表示する。

- 動作方針  
  - 既存取引の「修正」は、原則として新バージョンを作成する操作とし、過去バージョンは履歴として残る。  
  - 保存時には、入力内容をもとに取引の整合性チェックを行い、重大な不整合（例：期間反転、不正な組み合わせなど）は保存前に検出してユーザに知らせ、保存は却下される。  
  - 取引詳細画面では、  
    - バージョン履歴（旧版／新版の関係）  
    - 主要条件の差分（簡易でよい）  
    - スケジュール／キャッシュフロー参照（まずは表形式）  
    - 状態遷移の入口（取消／クローズ／行使など、v1では最低限）  
    を提供し、「説明可能性」と「運用上の確認作業」を支援する。

---

#### 3.4 マーケット管理（取込・QA・マーケット版・カーブ・Fixing）

- 全体目的  
  - 評価に用いるマーケットを「版」として管理し、取込 → 点検 → 確定（承認／ロック）→ カーブ構築までを一貫して行えるようにする。  
  - 「どの入力を採用し、どの版が評価に使われたか」を説明可能にする。

- マーケット取込（取込実行／再取込／失敗確認）  
  - 目的：外部データ（ファイル等）を受け取り、取込結果（成功／失敗、件数、エラー）を確認できるようにする。  
  - 方針：  
    - 取込の単位を明確にし、後から追跡できるよう履歴を残す。  
    - 失敗時に、同条件での再実行ができる（運用で必須）。

- クォート確認（QA・採用／除外）  
  - 目的：異常値や前日との差の大きい値を点検し、カーブ構築等に「採用する／除外する」を操作できるようにする。  
  - 方針：  
    - 参照（一覧・詳細）を中心にしつつ、採用可否の変更と理由メモを残せるようにする。  
    - フィルタ（異常のみ、特定通貨・特定テナー等）と差分表示（前回版／前日）を重視する。

- マーケット版の作成（コンポーズ）  
  - 目的：評価に用いる「ひとまとまりの入力」を選び、マーケット版として作成する。  
  - 方針：  
    - 版の作成時に、必要な入力の組み合わせを明示的に選べる（例：金利は新しいが為替は前回のまま、などの部分差し替えに対応）。  
    - 版にはメモを残し、後から説明できるようにする。

- マーケット版一覧・詳細（承認／ロックを含む）  
  - 一覧画面の方針：  
    - 評価日、版を識別するラベル、作成時刻、承認状態、ロック状態などの基本情報を一覧表示。  
    - 評価日や承認状態でのフィルタ、作成順・評価日順での並べ替え。  
    - 版の詳細画面への遷移、および承認状態の変更操作（承認フローの簡易版）を提供する。  
  - 詳細画面の方針：  
    - 版の概要（評価日、説明、作成者、メモなど）を上部にまとめて表示。  
    - 下部に、為替・金利・ボラティリティ・その他マーケット要素をタブで分割し、それぞれ表形式で表示する。  
    - v1では基本的に参照中心とし、一部の修正や上書きは極力避ける（修正が必要な場合は新しい版を作る運用を優先）。  
    - ロック（確定）操作を提供し、ロック後は内容が変更されないことを UI 上でも明確にする。

- カーブ構築（実行／履歴／確認）  
  - 目的：マーケット版を入力として、必要なカーブを構築し、評価に使える状態にする。  
  - 方針：  
    - 対象版と対象カーブを指定して「構築実行」できる。複数カーブをまとめて実行できる導線も用意する。  
    - 実行履歴（成功／失敗、所要時間、エラー）を確認できる。  
    - 結果の確認は v1 は表中心（点列、主要テナー、簡易な健全性チェック）。v2でグラフ表示や詳細なフィット誤差表示を拡張する。

- Fixing 管理（未確定一覧／確定入力／取込／承認）  
  - 目的：参照金利や指標の確定値（Fixing）が必要な取引について、未確定のものを洗い出し、確定させる。  
  - 方針：  
    - 「未確定 Fixing の一覧」を提供し、評価や日次処理の前提条件を満たしているか確認できるようにする。  
    - 手入力・取込の両方を想定し、確定後は履歴・メモを残す（簡易でよい）。  
    - v1は最低限の運用（確定・参照・メモ）に絞り、複雑な承認フローや突合は v2 以降で拡張する。

---

#### 3.5 評価実行・結果（実行監視／詳細／比較／出力）

- 評価実行画面  
  - 目的：評価日、利用するマーケット版、対象範囲（ブックや通貨など）、利用するシナリオ集合、算出する指標などを指定し、評価処理を実行する。  
  - 方針：  
    - 必須の入力はなるべく少なくし、デフォルトで「最新のロック済みマーケット版」「全ブック」などを選択候補とする。  
    - プリセット（よく使う評価条件）からワンクリックで起動できる導線を用意する。  
    - 実行後は 実行中・完了・エラーの状態を確認できる。

- 評価ジョブ一覧画面  
  - 目的：過去の評価ジョブを一覧表示し、どの条件でいつ評価したかを確認できるようにする。  
  - 方針：  
    - 評価日、マーケット版、シナリオ集合、ステータス、実行者、実行時刻などを一覧表示。  
    - クリックで個別のジョブ詳細／結果画面へ遷移できるようにする。  
    - 失敗のみの絞り込み、再実行（同条件／条件を少し変える）の導線を用意する。

- 評価ジョブ詳細画面  
  - 目的：「このジョブが何を対象に、何を計算したか」を説明可能にする。  
  - 方針：  
    - 入力条件（評価日、利用マーケット、対象範囲、指標、シナリオ等）を上部に固定表示する。  
    - 対象件数（取引数、ブック数など）と、実行状況（開始・終了・エラー）を明確にする。  
    - エラー時は、原因の概要と、どこまで進んだか（最低限）を表示する。

- 評価結果サマリ／明細画面  
  - 方針（v1）：  
    - v1 では表形式でのサマリ（ブック別・通貨別・商品別など）と取引別明細の参照を中心にする。  
    - フィルタ、並べ替え、トップ寄与（上位N件）など、日々の確認作業で必要な操作性を優先する。  
    - エクスポート（CSV 等）は v1 から提供する。  
  - 拡張（v2以降）：  
    - グラフ表示（時系列、バケット可視化等）や、より多様な集計軸を追加する。

- 出力（エクスポート／定型レポート）  
  - 方針：  
    - 画面で指定したフィルタ条件をそのまま出力できる。  
    - v1はエクスポートのみ、定型レポートは v2 以降で拡張する。

---

#### 3.6 設定・マスタ画面

- 目的  
  - 日々の運用で頻繁に変更される設定を UI 上から扱えるようにする。

- 編集対象の方針  
  - ポートフォリオ、シナリオ集合、評価条件プリセット、評価プロファイルなど、「運用ルールの変更で追加・修正される可能性が高いもの」を UI 編集対象とする。  
  - 指標定義（算出対象、感応度のバケット等）についても、参照と有効無効、簡易編集を提供し、運用で増える定義に追随できるようにする。  
  - 通貨・日数計算・営業日規則・カレンダーなどの基礎的な設定は、v1では参照専用とし、定義変更はスクリプトや別ツールで行う前提とする。

- 画面構成  
  - 一覧＋簡易編集ダイアログというパターンに揃え、テーブル形式での一括編集ではなく「1 行ずつ確実に修正する」操作感とする。  
  - 変更履歴やメモを表示できる領域を用意し、「なぜこの設定になっているか」の説明を残せるようにする。

---

### 4. 典型的な業務フローと UI の対応

#### 4.1 日次運用フロー（想定）

1. 当日（または前日EOD）にマーケットデータを取り込む（取込の成功／失敗を確認し、必要なら再取込）。  
2. クォート確認画面で異常値や前日差を点検し、必要に応じて採用／除外やメモを付与する。  
3. マーケット版を作成（必要な入力の組み合わせを選択）し、内容を確認する。  
4. マーケット版を承認し、確定版としてロックする（以後変更しないことを保証）。  
5. 対象のマーケット版に対してカーブ構築を実行し、結果を確認する（失敗時は原因確認→再実行）。  
6. 取引登録・修正（新バージョン作成）を行う。必要に応じて取引詳細でスケジュールや注意点を確認する。  
7. 未確定 Fixing がある場合は Fixing 管理で確定させる（評価の前提を満たす）。  
8. 評価起動画面から当日分の評価ジョブを起動する（プリセット起動も可）。  
9. ジョブ一覧／詳細で実行状況を監視し、失敗時は原因を確認して再実行する。  
10. 結果サマリ／明細でブック別・取引別の指標を確認し、必要に応じてエクスポートする。

#### 4.2 マーケット差し替え・検証（比較）フロー（想定）

1. 新しいマーケットデータを取り込み、新規のマーケット版を作成する（必要なら部分差し替え）。  
2. クォート確認・マーケット版詳細で点検し、承認／ロックする。  
3. 必要なカーブ構築を実行し、結果を確認する。  
4. 同一評価日で旧版・新版それぞれを用いた評価ジョブを実行する。  
5. 評価結果の比較画面で 2 ジョブを比較し、差分と主要因（寄与の大きい取引等）を確認する。  
6. 検証結果のメモと必要な出力を残す。

---

### 5. UI 技術・アーキテクチャの方針（抽象）

- フロントエンド  
  - SPA もしくは同等の体験を提供できる構成とし、一覧・検索・フィルタ・詳細遷移の操作が軽快であることを重視する。  
  - 大規模なデザインシステムの導入は v1 では不要だが、画面間でコンポーネント（テーブル、フィルタ、モーダル等）の一貫性を保つ。

- バックエンドとの役割分担  
  - UI は「参照・起動・状態確認・簡易編集」を担い、複雑な計算や大量データ処理はバックエンドに委譲する。  
  - ジョブ（取込・カーブ構築・評価）の状態は、UI から追跡できるようにする（実行中・完了・失敗）。

- ログ・監査  
  - 重要な操作（承認、ロック、採用／除外、Fixing確定、取引修正、評価起動等）は、誰がいつ行ったかが追跡できるようにする（v1は簡易でよい）。

---

### 6. UI 設計における優先順位

- v1 の優先度（高い順）  
  1. 取引一覧・取引登録・編集（＋取引詳細の最低限：履歴と参照）  
  2. マーケット取込・クォート確認（QA）・マーケット版作成／一覧／詳細・承認／ロック  
  3. カーブ構築の起動・履歴確認・結果の表形式点検  
  4. 評価起動・ジョブ一覧／詳細・結果サマリ／取引別明細・エクスポート  
  5. 評価結果の比較（簡易）  
  6. 設定のうちポートフォリオ・シナリオ・評価条件プリセットの簡易編集  
  7. Fixing 管理（最低限：未確定一覧と確定入力）  
  8. 管理者向けのユーザ・権限管理（最低限の機能）

- その後の拡張余地  
  - 評価結果のグラフ表示（感応度やストレスの可視化、時系列表示など）  
  - 評価結果の様々な粒度でのサマリ（ポートフォリオ、通貨、商品、リスク要因など）  
  - マーケットのグラフ表示（カーブ、ボラティリティサーフェス等）  
  - クォートの自動アラート強化（閾値・ルールのカスタマイズ）  
  - Fixing の突合・承認フローの強化  
  - 出力（定型レポート）と日次照合の支援機能
