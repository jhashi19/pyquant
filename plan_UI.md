
## UI 方針（Web アプリ）

### 1. UI 全体コンセプト

- 対象  
  - 個人利用・検証用途を主としつつ、小規模金融機関のフロント／ミドル業務でも日常的に使えるレベルを目指す。

- 目的  
  - 主な業務フロー（取引登録 → マーケット設定 → 評価実行 → 結果確認 → 分析）を、ブラウザ上で一貫して完結できるようにする。

- デザイン方針  
  - 見た目は「シンプルで整理されている」ことを最優先としつつ、余白・タイポグラフィ・色数を適度に使い、業務アプリとしてしょぼく見えない最低限の質感を確保する。  
  - 設定（ポートフォリオ・シナリオ・評価条件プリセット等）の変更は、できるだけ画面上から完結できるようにし、「コードを書き換えないと変えられない設定」を最小限に抑える。  
  - 対象は PC ブラウザのみとし、横幅のある画面での一覧表示や複数パネル配置を前提に設計する。  
  - 複雑な設定や詳細情報は、必要なときに展開できるよう折りたたみやタブで隠し、日々の運用に必要な最小限の情報を常に前面に出す。

---

### 2. 画面構成（グローバルなメニュー構造）

アプリ全体は、上部に固定されたメインメニューを持つ Web アプリとし、次の区分を基本とする。

1. ログイン
2. ダッシュボード  
3. 取引管理  
4. マーケット管理  
5. 評価・結果  
6. 設定・マスタ  
7. 管理（アドミニストレーター専用）

各区分に含める画面の粒度は以下とする（名称はあくまで方針レベル）。
- ログイン
  - ユーザのログイン画面。

- ダッシュボード  
  - 直近の評価結果、マーケットの状態、エラー／警告をまとめて表示するトップ画面。

- 取引管理  
  - 取引一覧画面  
  - 取引の登録・編集画面（商品ごとの入力フォーム）

- マーケット管理  
  - マーケット版の一覧画面  
  - 個々のマーケット版の詳細画面（為替・金利・ボラティリティ等をタブで切り替え）

- 評価・結果  
  - 評価ジョブの起動画面（評価条件の指定）  
  - 評価ジョブの履歴一覧画面  
  - 個々の評価ジョブの結果サマリ／明細画面

- 設定・マスタ  
  - 日常的に変更する設定（ブック、シナリオ集合、評価条件プリセットなど）の簡易編集画面  
  - 基礎的な設定（通貨・日数計算・カレンダー等）は原則参照専用

- 管理（アドミニストレーター専用）  
  - ユーザ管理画面（ユーザの追加・削除・無効化、パスワードリセットなど）  
  - 権限管理画面（ユーザごとのロール設定： v1では 通常ユーザ／管理者 のみとする）  
  - 本メニューはアドミニストレーター権限でログインした場合のみ表示・アクセス可能とし、一般ユーザには一切見えない構成とする。

---

### 3. 代表画面ごとの方針

#### 3.1 ログイン

- 目的
  - セキュリティの基本としてログインしたユーザのみが参照・操作ができる。

- 主な表示内容
  - ユーザID、パスワードの入力欄
  - ログインボタン

- 方針
  - パスワードはハッシュ化してテーブルに保持する。
  - パスワードのハッシュ化はソルト、ペッパー、ストレッチングを利用する。

---

#### 3.2 ダッシュボード

- 目的  
  - アプリにログインした直後に「現在の状態」を一目で把握できるようにする。

- 主な表示内容  
  - 直近数日の評価結果サマリ（評価日ごとの全体損益・残高など、主要指標）  
  - 最新のマーケット版の一覧（評価日・版のラベル・承認状態・ロック状態などの概要）  
  - 最新の評価ジョブの実行状況（成功／失敗、実行時刻、対象範囲）  

- 操作方針  
  - 詳細画面へのリンクを各カードや行に設置し、「何か気になったらすぐに掘れる」ことを重視する。  
  - 編集系操作は置かず、参照・遷移のハブとする。

---

#### 3.3 取引一覧画面

- 目的  
  - 登録済みの取引を検索・フィルタし、状態・期日・通貨等を俯瞰する。

- 主な機能  
  - 条件検索（ブック、商品種別、通貨、取引日／満期日、ステータスなどを組み合わせて絞り込み）  
  - ソート（残存期間、取引日、名目残高など）  
  - 行クリックで取引詳細画面へ遷移  
  - CSV 等でのエクスポート（ブック別・商品別の確認や Excel 分析向け）

- 方針  
  - 行数が多くなることを前提に、ページング・簡易集計（フッタでの合計・件数）を導入する。  
  - 商品固有の細かい条件は別画面で確認する。(スワップのキャッシュフロースケジュールなど)

---

#### 3.4 取引登録・編集画面

- 目的  
  - 新規取引の登録、および既存取引の修正（新バージョンとしての登録）を行う。

- UI 方針  
  - 画面は「商品ごとに 1 つの編集画面」を基本とし、共通項目と商品固有項目を分割したステップ形式ではなく、ひとつながりのレイアウトで一覧できるようにする。  
  - 同一画面内で、  
    - 上部：共通項目（ブック、通貨、名目、取引日、満期、方向など）  
    - 下部：商品固有項目（クーポン条件、オプション条件、スケジュール関連設定など）  
    を縦方向に配置し、「スクロールすれば一望できる」構成を目指す。  
  - 共通項目と商品固有項目は、画面上ではセクション見出し・枠で区切るにとどめ、画面遷移やウィザード形式では分割しない（戻る／進む操作が不要なシンプルな体験を優先）。  
  - 通貨・ブック・カレンダー・インデックスなど、選択肢が限定されるものはプルダウンで選び、コードの直接入力を最小限にする。  
  - 必須項目かどうかを視覚的に明示し、入力漏れがある場合は画面上部または項目近くにエラーメッセージを表示する。

- 動作方針  
  - 既存取引の「修正」は、原則として新バージョンを作成する操作とし、過去バージョンは履歴として残る（どのようにバージョン管理するかはデータ設計側の方針に従う）。  
  - 保存時には、入力内容をもとに取引の整合性チェックを行い、重大な不整合（例：期間反転、不正な組み合わせなど）は保存前に検出してユーザに知らせ、保存は却下される。

---

#### 3.5 マーケット版一覧・詳細画面

- 目的  
  - 評価に用いるマーケットの版を管理し、「どの版を使っているか」を常に説明可能にする。

- 一覧画面の方針  
  - 評価日、版を識別するラベル、作成時刻、承認状態、ロック状態などの基本情報を一覧表示。  
  - 評価日や承認状態でのフィルタ、作成順・評価日順での並べ替え。  
  - 版の詳細画面への遷移、および承認状態の変更操作（承認フローの簡易版）を提供する。

- 詳細画面の方針  
  - 版の概要（評価日、説明、作成者、メモなど）を上部にまとめて表示。  
  - 下部に、為替・金利・ボラティリティ・その他マーケット要素をタブで分割し、それぞれ表形式で表示する。
  - v1では基本的に参照中心とし、一部の修正や上書きは極力避ける（修正が必要な場合は新しい版を作る運用を優先）。

---

#### 3.6 評価実行・結果画面

- 評価実行画面  
  - 目的：評価日、利用するマーケット版、対象範囲（ブックや通貨など）、利用するシナリオ集合などを指定し、評価処理を実行する。  
  - 方針：  
    - 必須の入力はなるべく少なくし、デフォルトで「最新のロック済みマーケット版」「全ブック」などを選択候補とする。  
    - 実行後は 実行中・完了・エラーの状態を確認できる。

- 評価ジョブ一覧画面  
  - 目的：過去の評価ジョブを一覧表示し、どの条件でいつ評価したかを確認できるようにする。  
  - 方針：  
    - 評価日、マーケット版、シナリオ集合、ステータス、実行者、実行時刻などを一覧表示。  
    - クリックで個別の結果サマリ画面へ遷移できるようにする。

- 評価結果詳細画面  
  - 結果詳細画面は無し。基本的にはテーブルで確認。
  - v2ではファイル出力や評価日を指定してグラフ出力などの機能を追加

---

#### 3.7 設定・マスタ画面

- 目的  
  - 日々の運用で頻繁に変更される設定を UI 上から扱えるようにする。

- 編集対象の方針  
  - ブック・シナリオ集合・評価条件プリセットなど、「運用ルールの変更で追加・修正される可能性が高いもの」を UI 編集対象とする。  
  - 通貨・日数計算・営業日規則・カレンダーなどの基礎的な設定は、v1では参照専用とし、定義変更はスクリプトや別ツールで行う前提とする。

- 画面構成  
  - 一覧＋簡易編集ダイアログというパターンに揃え、テーブル形式での一括編集ではなく「1 行ずつ確実に修正する」操作感とする。  
  - 変更履歴やメモを表示できる領域を用意し、「なぜこの設定になっているか」の説明を残せるようにする。

---

### 4. 典型的な業務フローと UI の対応

#### 4.1 日次運用フロー（想定）

1. 前日または当日朝にマーケットデータを取り込み、新しいマーケット版を作成する。  
2. マーケット版詳細画面で内容を確認し、承認状態を「評価に使用してよい」状態に変更する。  
3. 取引登録画面から新規取引や変更取引を登録（前日の続きも含む）。  
4. 評価起動画面から当日分の評価ジョブを起動する。  
5. ジョブ完了後、評価結果詳細画面でブック別・取引別の指標を確認する。  
6. 必要に応じて結果をエクスポートし、外部レポートや分析に利用する。

このフローを plan に明記しておき、詳細設計・実装時には画面・API をこの流れに沿わせる。

#### 4.2 マーケット版の差し替え・比較フロー（想定）

1. 新しいマーケットデータを取り込み、新規のマーケット版を作成する。  
2. マーケット版詳細画面から異常値チェックや QA コメント入力を行う。  
3. 承認後、同じ評価日で旧版と新版本を指定して、それぞれ評価ジョブを実行する。  
4. 評価ジョブ一覧画面から 2 つのジョブを選択し、差分を確認する（将来的には差分専用ビューを追加する余地を残す）。

---

### 5. UI 技術・アーキテクチャの方針（抽象）

- フロントエンド  
  - 画面遷移や状態管理を見通し良く実装できる構成（シングルページアプリケーションに近い構成）を想定。 
  - 共通コンポーネント（検索フォーム、一覧テーブル、ダイアログ、タブなど）を設計し、新機能追加時も同じパターンで UI を組めるようにする。  
  - PC ブラウザでの利用を前提とし、横方向のスペースを活かしたレイアウト（一覧＋詳細、一覧＋サマリなど）を採用する。

- バックエンドとの境界  
  - UI からは REST/JSON ベースの API を呼び出し、データベースには直接アクセスしない。  
  - API のリソースは「取引」「マーケット版」「評価ジョブ」「評価結果」「マスタ設定」「ユーザ／権限」といったドメイン単位で定義し、具体的なテーブル設計とは切り離して考える。  
  - データ取得 API は一覧・詳細の 2 パターンを基本とし、集計結果は必要に応じて別エンドポイント（サマリ用 API）で提供する。

---

### 6. UI 設計における優先順位

- v1 の優先度（高い順）  
  1. 取引一覧・取引登録・編集  
  2. マーケット版一覧・詳細・承認  
  3. 評価起動・評価結果サマリ／取引別明細  
  4. 設定のうちポートフォリオ・シナリオの簡易編集  
  5. 管理者向けのユーザ・権限管理（最低限の機能）

- その後の拡張余地  
  - 評価結果のグラフ表示（感応度やストレステストの時系列表示など）
  - 評価結果の様々な粒度でのサマリ（ポートフォリオ、通貨、商品など）
  - マーケットのグラフ表示（カーブ・ボラティリティサーフェスなど）  
  - 評価結果とマーケットを組み合わせた結果表示
